spring:
  application:
    name: dkl-auth
  cloud:
    nacos:
      config:
        server-addr: ${nacos.url}:8848
        group: DEFAULT_GROUP
        prefix: dkl-auth
        file-extension: yaml
      discovery:
        server-addr: ${nacos.url}:8848
