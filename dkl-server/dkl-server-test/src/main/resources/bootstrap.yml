spring:
  application:
    name: dkl-server-test
  cloud:
    nacos:
      config:
        server-addr: ${nacos.url}:8848
        group: DEFAULT_GROUP
        prefix: dkl-server-test
        file-extension: yaml
      discovery:
        server-addr: ${nacos.url}:8848
