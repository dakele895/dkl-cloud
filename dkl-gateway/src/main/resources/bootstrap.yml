spring:
  application:
    name: dkl-gateway
  cloud:
    nacos:
      config:
        server-addr: ${nacos.url}:8848
        group: DEFAULT_GROUP
        prefix: dkl-gateway
        file-extension: yaml
      discovery:
        server-addr: ${nacos.url}:8848